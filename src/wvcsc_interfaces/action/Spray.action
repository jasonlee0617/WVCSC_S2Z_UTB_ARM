# 底层喷嘴喷洒 Action。SprayTask 仅在正常视觉对准或明确允许的回退策略完成后调用。
# SprayActuator 是服务端，拥有继电器通道 2 和定时器；继电器开启命令确认且定时区间开始后，
# 才发布 /spray/simulated_active=true。该 Action 独立于车辆行进时的广域喷洒通道 1。
# Goal: 一次病态目标喷嘴执行请求；不携带路线 Point ID 或植物 ID。
string mission_id                     # 路线或独立测试任务关联 ID。
float32 duration                      # 本次目标喷嘴请求开启时长（秒）。
string mode                           # 执行模式；当前项目通常使用 continuous（连续）。
---
# Result: 执行器关闭或清理完成后的软件执行结果，不表示液体流量已经测量确认。
uint16 OK=0                           # 请求的定时喷洒周期已完成。
uint16 INVALID_GOAL=1                 # 时长或 mode 验证失败。
uint16 BUSY=2                         # 前一喷嘴周期仍处于活跃状态。
uint16 CANCELED=3                     # 取消已触发清理和继电器关闭请求。
uint16 EMERGENCY_STOPPED=4            # 安全/紧急路径中断了喷洒。
uint16 RELAY_FAILED=5                 # 通道 2 的继电器命令未获得软件确认。
bool success                           # 仅当 error_code=OK 时为 True。
uint16 error_code                      # 上述数值错误码；请保留既有数值兼容性。
string message                         # 供日志和 UI 显示的执行器诊断文本。
float32 actual_duration                # 本地定时器测得的开启区间（秒），不是流量测量值。
---
# Feedback: 活跃喷嘴周期进度。即使实现不发布 CLOSING，该常量仍保留数值兼容性。
uint8 ACTIVE=0                         # 继电器开启已获软件确认，定时器正在运行。
uint8 CLOSING=1                        # 保留的可选继电器关闭/清理阶段。
uint8 phase                            # 当前反馈阶段常量。
float32 elapsed                        # 已经过的喷嘴开启时间（秒）。
float32 progress                       # 供 UI 显示的归一化周期进度。
