# Qt 向 MissionManager 加载手动路线的服务。Qt 在完成 RViz 录点和任务编辑后
# 调用本服务；MissionManager 校验坐标系、路线点几何、点类型和数量，并发布 READY
# 状态。本服务只加载任务，不会隐式启动 Nav2 或喷洒；后续由 /mission/start 触发。
# Request: header.frame_id 下的完整可编辑任务输入，当前完整任务使用 map。
std_msgs/Header header                 # 公共坐标系和请求时间戳。
string mission_id                      # Qt 生成的本次任务标识符。
geometry_msgs/Pose home_pose           # header.frame_id 下的车辆 HOME 位姿。
bool return_home_after_finish          # 路线完成后是否导航回 HOME。
wvcsc_interfaces/ManualMissionTarget[] targets  # 按执行顺序排列的路线点。
---
# Response: 仅表示任务是否已成功加载；实际启动仍是独立的 /mission/start 调用。
bool success                           # 校验并更新 MissionManager 状态后为 True。
string message                         # Qt 显示的校验失败原因或加载摘要。
