# Qt 向 MissionManager 加载手动路线的服务。Qt 完成 RViz 录点和路线编辑后调用；
# MissionManager 校验坐标系、Point 几何、分类和数量，成功后发布 READY。
# 本服务只加载任务：不会隐式启动 Nav2、广域喷洒或机械臂喷洒；后续必须由 /mission/start 触发。
# Request: header.frame_id 下完整、可编辑的路线输入；当前完整任务通常使用 map。
std_msgs/Header header                 # 所有 Pose/Point 共享的坐标系和请求时间戳。
string mission_id                      # Qt 生成的本次路线任务标识符。
geometry_msgs/Pose home_pose           # header.frame_id 下的车辆 HOME 位姿。
bool return_home_after_mission         # 最后一个 Point 完成后是否导航回 HOME。
wvcsc_interfaces/ManualMissionPoint[] points  # 按执行顺序排列的路线 Point；最后一项自然结束任务。
---
# Response: 仅表示路线是否已成功加载；实际运行仍需要独立的 /mission/start 调用。
bool success                           # 所有校验通过且 MissionManager 已更新运行前状态时为 True。
string message                         # 供 Qt 显示的校验失败原因或加载摘要。
