# 校准喷嘴瞄准点计算服务。SprayTask 在选定病果目标后，且在调用 AlignTarget 之前调用此服务。
# 服务端返回一个固定正工作平面距离下的目标像素点；它不执行任何机械臂动作、
# 视觉伺服指令或继电器操作，因此调用方保留执行控制权。
# Request: 距离配置标定的工作平面距离（米）。
float32 working_range_m                # 必须为有限值且严格为正；此处 0 为无效值。
---
# Response: 期望的全图像素喷嘴轴线像素点以及用于验证该点的图像尺寸。
# success=false 时，调用方应据此判断不进入正常视觉伺服流程。
bool success                           # 当标定/距离解析成功时为 True。
float32 desired_u_px                   # 在返回的图像帧中的水平目标像素点。
float32 desired_v_px                   # 在返回的图像帧中的垂直目标像素点。
uint32 image_width                     # 用于约束 desired_u_px 的图像宽度。
uint32 image_height                    # 用于约束 desired_v_px 的图像高度。
string message                         # 人类可读的标定/校验细节。