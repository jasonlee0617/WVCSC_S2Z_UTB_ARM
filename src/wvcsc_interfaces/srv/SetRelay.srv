# MissionManager 与 SprayActuator 共用的继电器命令服务。
# 完整任务中通道 1 属于车辆行进时的广域喷洒，通道 2 属于机械臂喷嘴；通道 2 的正常定时
# 开启动作由 SprayActuator 持有，MissionManager 仅在终止/清理等全局路径直接关闭全部通道。
# 响应 success 只确认软件命令已被继电器后端接受/处理，不表示独立触点、泵、压力或液体流量
# 已获得物理测量确认。
# Request: 继电器输出通道、期望状态和可选定时长度。
uint8 channel                          # 项目约定：1=广域喷洒，2=机械臂喷嘴。
bool enabled                           # True 请求开启；False 请求立即关闭。
# 定时开启时长（秒）。enabled=true 且 duration=0 表示持续开启，直至收到显式 False；
# enabled=false 时 duration 应为 0，表示普通立即关闭请求。
float32 duration
---
# Response: 调用方只能在 success=true 后更新自身的软件确认活跃状态。
bool success                           # 继电器后端接受并处理命令时为 True；不表示定时已结束或物理流量已确认。
string message                         # 供日志和 UI 使用的后端特定诊断文本。
